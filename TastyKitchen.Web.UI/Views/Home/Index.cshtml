
@model IEnumerable<TastyKitchen.Web.UI.Models.DailyExpense>

@{
    ViewData["Title"] = "Home Page";
}
<h2>Daily Expenses</h2>
<p>
    <a asp-action="Create">Add Expense</a>
</p>
<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Quantity)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.Unit)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Amount)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Date)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Quantity)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Unit)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Date)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>

            }
            @using (Html.BeginForm("Index", "Home", new { pageIndex = @ViewBag.PreviousPage }, FormMethod.Post))
            {
                <button class="btn btn-dark">Previous</button>
            }
            @using (Html.BeginForm("Index", "Home", new { pageIndex = @ViewBag.nextPage }, FormMethod.Post))
            {
                <button class="btn btn-dark">Next</button>
            }
        </tbody>
    </table>
</div>


@*<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DailySale.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DailySale.Amount)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.DailySale.Date)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DailySale.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DailySale.Amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DailySale.Date)
                    </td>
                    <td>
                        <a asp-action="EditDailySale" asp-route-id="@item.DailySale.Id">Edit</a> |
                        <a asp-action="DetailsDailySale" asp-route-id="@item.DailySale.Id">Details</a> |
                        <a asp-action="DeleteDailySale" asp-route-id="@item.DailySale.Id">Delete</a>
                    </td>
                </tr>

            }
            @using (Html.BeginForm("Index", "Home", new { pageIndex = @ViewBag.DailySalePreviousPage }, FormMethod.Post))
            {
                <button>Previous</button>
            }
            @using (Html.BeginForm("Index", "Home", new { pageIndex = @ViewBag.DailySaleNextPage }, FormMethod.Post))
            {
                <button>Next</button>
            }
        </tbody>
    </table>
</div>*@
